# 系统/缓存
.DS_Store
__pycache__/
*.pyc
*.pyo

# 密钥(永不入库)
config/secrets.yaml
config/secrets.yml
config/share_tokens.json
.env
.dev.vars
*.key
*.token

# 第三方 MCP server(用 scripts/setup-mcp.sh 重新安装,不入库)
vendor/

# 本地反馈(生产反馈存在 Cloudflare R2;本地 dev server 才写这里)
data/feedback.jsonl

# Node / wrangler 本地产物(不入库)
node_modules/
.wrangler/
.mf/

# 灰度构建产物(deploy-gray.sh 输出到这里,不入库)
build/
